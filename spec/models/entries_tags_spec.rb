require 'spec_helper'

describe EntriesTags do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    EntriesTags.create!(@valid_attributes)
  end
end
